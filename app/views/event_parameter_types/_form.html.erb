<%= form_for(@event_parameter_type) do |f| %>
  <% if @event_parameter_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_parameter_type.errors.count, "error") %> prohibited this event_parameter_type from being saved:</h2>

      <ul>
      <% @event_parameter_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
