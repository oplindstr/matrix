<div class="box">

  <% if @document_group_id %>
		<h1 class="title"><%= t('new_document_for_group') %> <%= @document_group.get_name %></h1>
	<% else %>
		<h1 class="title"><%= t('new_document') %></h1>
	<% end %>

  <%= render 'form' %>

</div>
