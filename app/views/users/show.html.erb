<h2>Muokkaa omia tietoja</h2>

<% if not @member %>
  <p>Jäsenmaksuasi tälle lukuvuodelle ei ole merkitty maksetuksi. Älä välitä tästä, jos olet maksanut sen.</p>
<% end %>

<%= render 'form' %>

<% if @user.player %>
    <h2>Huutopussiratingit</h2>

    <p>Olet sijalla <%= @user.player.placement %></p>

    <p>Odotusarvo: <%= @user.player.expectation.round(2) %></p>

    <p>Keskihajonta: <%= @user.player.deviation.round(2) %></p>

    <p>Vahvuus: <%= [(@user.player.expectation - 3*@user.player.deviation).round(2), 0].max %></p>

    <%= form_for(@player) do |f| %>
        <div class="field">
          <%= f.label :private, 'Älä näytä pisteitäni pääsivulla' %>
          <%= f.check_box :private %>
        </div>
        <%= f.submit :value => 'Tallenna' %>
    <% end %>

    <br>

<% end %>

<% if sub_admin %>

  <%= form_for @user, :url => { :action => "add_picture" } do |f| %>

    <div class="form-area">

      <h2>Lisää kuva</h2>

      <div class="field">
        <%= f.file_field :avatar %>
      </div>

    </div>

    <div class="actions">
      <%= f.submit :value => 'Tallenna'  %>
    </div>

  <% end %>

<% end %>


<%= link_to 'Vaihda salasana', new_password_path(@user) %>
